parameters:
  - name: user_id
    type: int
    required: true
tags:
  - feed
responses:
  200:
    description: return feed id
    schema:
      id: Setup Channel
      properties:
        result:
          type: string
        code:
          type: string
        msg:
          type: string
        data:
          type: object
          properties:
            feed_id:
              type: integer